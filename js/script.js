// script.js
function updateProgressBar(progress) {
    document.querySelector('#progress-bar progress').value = progress;
}

// Example of setting the progress value
updateProgressBar(70); // Set progress to 70%
